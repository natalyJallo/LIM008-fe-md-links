import { converterPathAbsolute, walkTheDirectoryFilter } from '../src/module.js';

describe('converterPathAbsolute', () => {
  it('debería ser una función', () => {
    expect(typeof converterPathAbsolute).toBe('function');
  });
  it('Debería poder convertir una ruta relativa en absoluta', () => {
    expect(converterPathAbsolute('path')).toEqual('path absolute');
  });
});

describe('walkTheDirectoryFilter', () => {
  it('debería ser una función', () => {
    expect(typeof walkTheDirectoryFilter).toBe('function');
  });
  it('Debería poder convertir una ruta relativa en absoluta', () => {
    expect(walkTheDirectoryFilter('')).toEqual(outPut);
  });
});

describe('walkTheDirectoryFilter', () => {
  it('debería ser una función', () => {
    expect(typeof walkTheDirectoryFilter).toBe('function');
  });
  it('Debería poder convertir una ruta relativa en absoluta', () => {
    expect(walkTheDirectoryFilter('')).toEqual(outPut);
  });
});

describe('walkTheDirectoryFilter', () => {
  it('debería ser una función', () => {
    expect(typeof walkTheDirectoryFilter).toBe('function');
  });
  it('Debería poder convertir una ruta relativa en absoluta', () => {
    expect(walkTheDirectoryFilter('')).toEqual(outPut);
  });
});

describe('walkTheDirectoryFilter', () => {
  it('debería ser una función', () => {
    expect(typeof walkTheDirectoryFilter).toBe('function');
  });
  it('Debería poder convertir una ruta relativa en absoluta', () => {
    expect(walkTheDirectoryFilter('')).toEqual(outPut);
  });
});

describe('walkTheDirectoryFilter', () => {
  it('debería ser una función', () => {
    expect(typeof walkTheDirectoryFilter).toBe('function');
  });
  it('Debería poder convertir una ruta relativa en absoluta', () => {
    expect(walkTheDirectoryFilter('')).toEqual(outPut);
  });
});

describe('walkTheDirectoryFilter', () => {
  it('debería ser una función', () => {
    expect(typeof walkTheDirectoryFilter).toBe('function');
  });
  it('Debería poder convertir una ruta relativa en absoluta', () => {
    expect(walkTheDirectoryFilter('')).toEqual(outPut);
  });
});
